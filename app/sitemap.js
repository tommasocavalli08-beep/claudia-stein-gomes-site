import {SITE_URL} from '@/lib/site';export default function sitemap(){const paths=['','/sobre','/tratamentos/endolaser','/tratamentos/escleroterapia','/tratamentos/laser-transdermico','/tratamentos/linfedema','/publicacoes','/contato','/politica-de-privacidade'];return paths.map((path,i)=>({url:`${SITE_URL}${path}`,lastModified:new Date(),changeFrequency:i===0?'weekly':'monthly',priority:i===0?1:(path.startsWith('/tratamentos')?0.9:0.7)}));}
